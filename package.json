{
  "name": "basketball-stats-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "web/basketball-stats-web",
    "mobile/BasketballStatsMobile"
  ],
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck && npm run typecheck:convex",
    "typecheck:convex": "tsc --noEmit -p convex/tsconfig.json",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "full_check": "npm run typecheck && npm run lint && npm run format"
  },
  "devDependencies": {
    "convex": "^1.31.5",
    "prettier": "^3.8.0",
    "turbo": "^2.7.4"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.90",
    "typescript": "^5.9.3",
    "web-push": "^3.6.7"
  },
  "overrides": {
    "nativewind": {
      "tailwindcss": "3.2.7"
    },
    "tailwindcss": "3.2.7"
  },
  "packageManager": "npm@10.9.2"
}
